# Logs
logs
*.log
# 演示样例日志是交付物，不随 *.log 忽略
!sample-data/*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & build output
node_modules
dist
dist-ssr
*.local

# Rust / Tauri
/target/
src-tauri/target/
src-tauri/gen/
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local AI assistant tool configs (personal, not part of the project)
.agent/
.claude/
.codebuddy/
.codex/
.continue/
.cursor/
.gemini/
.kiro/
.opencode/
.qoder/
.roo/
.trae/
.windsurf/
.github/prompts/

# ZCode workspace config (local only; the bridge skill deliverable lives in skills/)
.zcode/*

# Internal planning docs (not for public release)
docs/*-plan.md

# 源码打包产物
serial-tool-*-src.zip

# 本地工作草稿（评审原型/计划文档，不进仓库）
/demo/
/docs/plan-*.md

# 发布说明草稿（正文只进 GitHub Releases，不进仓库）
/release-notes-*.md

# 性能诊断导出（DockMonitor 导出产物，本地排查用）
sample-data/perf-diag-*.json

# 单实例锁（运行时产物，lib.rs SingleInstance 在 cwd 生成）
/serial_tool-single-instance-lock

# 本地调研工具产物（个人，不进仓库）
docs/superpowers/
